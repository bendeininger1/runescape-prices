resources:
  pipelines:
    pipeline_latest_prices_raw_dlt:
      name: latest_prices_raw_dlt
      configuration:
        catalog: "${var.catalog}"
      libraries:
        - glob:
            include: ../latest_prices_raw_ETL_pipeline/**
      schema: default
      development: "${var.dev_flag}"
      photon: true
      catalog: "${var.catalog}"
      serverless: true
      root_path: ../latest_prices_raw_ETL_pipeline
